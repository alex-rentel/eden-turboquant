# Archived v0.6.0 benchmark artifacts

These files were the version-pinned benchmark harness and result set from
v0.6.0. They are kept here for historical comparison only and are not
maintained against the current API. For the live benchmark suite see
`benchmarks/run_full_suite.py` and `results/`.
